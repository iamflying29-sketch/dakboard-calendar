{
  "name": "dakboard-calendar",
  "private": true,
  "version": "1.0.0",
  "description": "Build tooling for the DAKboard Guest Bathroom weather/calendar widgets.",
  "scripts": {
    "build:weather": "node scripts/build-weather.mjs"
  },
  "devDependencies": {
    "esbuild": "^0.28.2"
  }
}
